<Window x:Class="BookingApp.View.FollowingTourLive"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="FollowingTourLive" Height="700" Width="600">
    <Grid>
        <Label x:Name="Followingtour">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Following tour live" FontSize="40" VerticalAlignment="Center" Margin="80,50,0,0" Width="400" Height="85" />
            </StackPanel>
        </Label>
        <Label Content="{Binding TourInstance.BaseTour.Title}" FontSize="55" Margin="146,211,146,0" VerticalAlignment="Top" Height="62" 
             />
        <ItemsControl ItemsSource="{Binding TourInstance.BaseTour.KeyPoints}" HorizontalAlignment="Center" Width="695">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding Status}" IsEnabled="{Binding IsEnabled}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </Grid>
</Window>
