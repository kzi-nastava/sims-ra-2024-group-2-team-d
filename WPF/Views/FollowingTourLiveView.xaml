<Window x:Class="BookingApp.WPF.Views.FollowingTourLiveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views"
        mc:Ignorable="d"
        Title="FollowingTourLiveView" Height="450" Width="800">
    <StackPanel>
        <TextBlock>Trenutna pozicija</TextBlock>
        <TextBlock Text="{Binding CurrentKeyPoint.Name}"></TextBlock>
        <TextBlock>Neke info o turi</TextBlock>
        <TextBlock Text="{Binding ActiveTour.BaseTour.Title}"></TextBlock>
        <TextBlock Text="{Binding ActiveTour.Date}"></TextBlock>
    </StackPanel>
</Window>
