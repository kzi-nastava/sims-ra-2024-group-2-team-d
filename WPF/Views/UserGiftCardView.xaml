<Window x:Class="BookingApp.WPF.Views.UserGiftCardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views"
        mc:Ignorable="d"
        Title="UserGiftCardView" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="My vouchers" FontSize="26" HorizontalAlignment="Center" FontWeight="Bold"></TextBlock>
        <ListBox ItemsSource="{Binding GiftCards}">
            
            <ListBox.ItemTemplate>
                    <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="10,10,10,10" Text="{Binding ReceiveDate, StringFormat='Receive Date: {0:yyyy-MM-dd}'}"/>
                        <TextBlock Margin="10,10,10,10" Text="{Binding ExpirationDate, StringFormat='Expiration Date: {0:yyyy-MM-dd}'}"/>
                    </StackPanel>
                        
                    </DataTemplate>
                        
                    </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>
